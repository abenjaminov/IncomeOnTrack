{
  "name": "income-on-track",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rimraf ./packages/server/dist && yarn workspace @income-on-track/server build",
    "start": "yarn workspace @income-on-track/server start"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "typescript": "^4.9.5"
  }
}
