{
  "name": "income-on-track",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "heroku-postbuild": "yarn && yarn workspace @income-on-track/server build",
    "start": "yarn workspace @income-on-track/server start",
    "serve": "cd ./packages/server && yarn dev",
    "client": "cd ./packages/client && yarn dev"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "typescript": "^4.9.5"
  }
}
