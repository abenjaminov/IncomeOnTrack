import {style} from '@vanilla-extract/css';

const classes = {
    button: style({
        width: '100%',
        height: '100%',
        borderWidth: '1px',
        borderStyle: 'solid',
    })
}

export default classes;
