import { style } from '@vanilla-extract/css';

const classes = {
    loginLayout: style({
        height: '100%',
        width: '100%',
        position: 'relative',
        backgroundColor: 'red'
    })
}

export default classes;
