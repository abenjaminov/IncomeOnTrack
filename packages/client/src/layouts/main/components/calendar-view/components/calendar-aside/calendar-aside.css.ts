import {style} from "@vanilla-extract/css";

export const calendarAsideContainer = style({
  width: '20%',
  height: '100%',
  padding: '0 0 0 .5rem',
  display: 'flex',
  flexDirection: 'column',
})

export const calendarAsidePicker = style({

})
