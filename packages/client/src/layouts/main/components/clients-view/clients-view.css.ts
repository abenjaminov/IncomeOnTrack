import { style } from '@vanilla-extract/css';

export const classes = {
    clientsView: style({
        height: '100%',
        width: '100%',
        padding: '3rem'
    })
}

export default classes;
